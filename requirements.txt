streamlit>=1.30.0
python-dotenv>=1.0.0
openai>=1.0.0
# GitHub Copilot SDK (no API key — auth via `gh auth login`)
# Install: pip install "git+https://github.com/github/copilot-sdk.git#subdirectory=python"
